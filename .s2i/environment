# Variables de entorno para el proceso Source-to-Image (S2I)
# Este archivo debe estar en .s2i/environment en la raíz de tu proyecto

# Argumentos de Maven para el build
MAVEN_ARGS=clean package -DskipTests -Dquarkus.package.type=fast-jar

# Opciones de Maven
MAVEN_OPTS=-Xmx1024m -XX:+UseParallelGC

# Configuración de Java para el build
JAVA_TOOL_OPTIONS=-XX:+UseParallelGC -XX:MinHeapFreeRatio=10 -XX:MaxHeapFreeRatio=20

# Habilitar builds incrementales para mayor velocidad
INCREMENTAL=true

# Configuración específica de Quarkus
QUARKUS_PACKAGE_TYPE=fast-jar

# JAR principal de la aplicación
JAVA_APP_JAR=/deployments/quarkus-run.jar

# Configuración de locale para evitar warnings
LANG=C.UTF-8
LC_ALL=C.UTF-8

# Configuración de Quarkus para contenedores
QUARKUS_HTTP_HOST=0.0.0.0
QUARKUS_HTTP_PORT=8080